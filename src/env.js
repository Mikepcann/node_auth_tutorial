/**
 * loads the .env file contents into the process.env var
 * When using this file, make sure that the .env is in the git ignore file.
 *  */ 

import dotenv from "dotenv"; // imports the dotenv pkg
dotenv.config(); // configure the environment
